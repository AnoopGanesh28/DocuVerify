Enterprise RAG Knowledge Base – A web app that lets organizations upload documents and query them using an LLM with Retrieval-Augmented Generation (RAG). It uses vector search to fetch relevant context and generate accurate, grounded answers, reducing hallucinations and manual document search.

