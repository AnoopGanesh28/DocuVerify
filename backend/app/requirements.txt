# Web Framework & Server
fastapi==0.110.0
uvicorn[standard]==0.27.1
python-multipart==0.0.9

# Settings
pydantic-settings==2.2.1
email-validator>=2.0         # required by Pydantic's EmailStr

# Database (SQLite default – no extra driver needed)
sqlalchemy>=2.0.36

# PostgreSQL driver (optional – only needed if you switch to PostgreSQL)
# psycopg2-binary==2.9.9

# Authentication (JWT + password hashing)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1                  # passlib 1.7.4 requires bcrypt<4.1 on Python 3.13

# Vector Database & Embeddings
chromadb>=0.5.0
sentence-transformers==2.5.1

# Document parsing
PyPDF2==3.0.1

# HTTP client (for Ollama calls)
requests==2.31.0

# Utilities
python-dotenv==1.0.1